run:
  timeout: 3m

linters:
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - gofmt
    - misspell
    - revive
    - unconvert

linters-settings:
  errorlint:
    asserts: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - bodyclose
        - errcheck
